{% set name = "miniopy-async" %}
{% set version = "1.20" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 56d7cac2efd07b7ba8ea09bff6478f2e80c80f29b08b4632f0b662b129b2ec35

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7
    - certifi
    - aiofile
    - aiohttp
    - urllib3

test:
  imports:
    - miniopy_async
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/hlf20010508/miniopy-async
  summary: Asynchronous MinIO Client SDK for Python
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - hlf20010508
